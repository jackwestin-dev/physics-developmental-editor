# Optional: only needed for "Generate excerpt" to call Claude.
# Get a key from https://console.anthropic.com/
# On Vercel: set ANTHROPIC_API_KEY in Project → Settings → Environment Variables.
ANTHROPIC_API_KEY=
