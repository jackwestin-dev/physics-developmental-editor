# Optional: only needed for "Generate excerpt" to call OpenAI.
# Get a key from https://platform.openai.com/api-keys
# On Vercel: set OPENAI_API_KEY in Project → Settings → Environment Variables.
OPENAI_API_KEY=
